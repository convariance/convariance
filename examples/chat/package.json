{
  "name": "convariance-chat-example",
  "version": "0.0.0",
  "private": true,
  "description": "Live-chat with Claude Code via the convariance bridge — a static SPA (GitHub Pages-ready) built on @convariance/client.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@convariance/client": "workspace:^"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
