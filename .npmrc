# The @silkweave packages peer on @clack/prompts (their logger) without the
# gateway needing to know — auto-install satisfies it, matching upstream's
# intended layout.
auto-install-peers=true
strict-peer-dependencies=false
